require 'spec_helper'

describe Activesupport::CallbackLog do
  it 'has a version number' do
    expect(Activesupport::CallbackLog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
